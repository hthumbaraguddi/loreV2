/**
 * App version — bump the patch number on every meaningful commit.
 * Format: MAJOR.MINOR.PATCH
 *   MAJOR — breaking changes / major redesigns
 *   MINOR — new features
 *   PATCH — bug fixes / small changes (bump this every push so you can verify production)
 *
 * To verify a deployment: check Settings → About for this string.
 */
export const APP_VERSION = '2.7.0';
